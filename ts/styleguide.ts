import { StyleSheet } from "react-native";

export const colors = {
  white: "#fff",
  gray: "#D6DADE",
  grayer: "#87939F",
};

export const font = StyleSheet.create({
  h1: {
    fontSize: 30,
  },
  h2: {
    fontSize: 20,
  },
  h3: {
    fontSize: 15,
  },
});
